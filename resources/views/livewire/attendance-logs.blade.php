<div><div class="toolbar"><div class="search"><span>⌕</span><input wire:model.live.debounce.300ms="search" placeholder="Search student or ID..."></div><input class="date-input" type="date" wire:model.live="date"></div><div class="card table-card"><div class="card-head"><div><span class="eyebrow">Audit trail</span><h3>Attendance logs</h3></div><span class="badge">{{ $logs->total() }} records</span></div><div class="table-scroll"><table><thead><tr><th>Student</th><th>Date</th><th>Time in</th><th>Time out</th><th>Duration</th><th>Status</th></tr></thead><tbody>@forelse($logs as $log)<tr><td><div class="student-cell"><span class="avatar">{{ strtoupper(substr($log->student->first_name,0,1).substr($log->student->last_name,0,1)) }}</span><div><b>{{ $log->student->full_name }}</b><small>{{ $log->student->student_id }}</small></div></div></td><td>{{ $log->attendance_date->format('M d, Y') }}</td><td>{{ $log->time_in->format('h:i A') }}</td><td>{{ $log->time_out?->format('h:i A') ?? '—' }}</td><td>{{ $log->time_out ? $log->time_in->diffForHumans($log->time_out,true) : 'On campus' }}</td><td><span class="pill {{ $log->status }}">● {{ ucfirst($log->status) }}</span></td></tr>@empty<tr><td colspan="6" class="empty">No attendance records found.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $logs->links() }}</div></div></div>
