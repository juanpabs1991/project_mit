<div>@if(session('success'))<div class="flash">✓ {{ session('success') }}</div>@endif<div class="toolbar"><div class="search"><span>⌕</span><input wire:model.live.debounce.300ms="search" placeholder="Search by name or student ID..."></div><button class="primary" wire:click="create">＋ Add student</button></div>@if($showForm)<div class="modal-backdrop"><form wire:submit="save" class="modal"><div class="card-head"><div><span class="eyebrow">Student record</span><h3>{{ $editingId?'Edit student':'Add a new student' }}</h3></div><button type="button" class="icon-button" wire:click="resetForm">×</button></div><div class="form-grid"><label>Student ID<input wire:model="student_id" placeholder="2026-0001">@error('student_id')<small class="error">{{ $message }}</small>@enderror</label><label>Email<input wire:model="email" type="email" placeholder="student@school.edu"></label><label>First name<input wire:model="first_name"></label><label>Last name<input wire:model="last_name"></label><label>Course<input wire:model="course" placeholder="BS Information Technology"></label><label>Year level<select wire:model="year_level">@foreach(range(1,6) as $y)<option value="{{ $y }}">Year {{ $y }}</option>@endforeach</select></label><label>Status<select wire:model="status"><option value="active">Active</option><option value="inactive">Inactive</option></select></label></div><div class="modal-actions"><button type="button" class="secondary" wire:click="resetForm">Cancel</button><button class="primary">{{ $editingId?'Save changes':'Add student' }}</button></div></form></div>@endif<div class="card table-card"><div class="card-head"><div><span class="eyebrow">Directory</span><h3>All students</h3></div><span class="badge">{{ $students->total() }} students</span></div><div class="table-scroll"><table><thead><tr><th>Student</th><th>ID number</th><th>Course</th><th>Year</th><th>Status</th><th></th></tr></thead><tbody>@forelse($students as $student)<tr><td><div class="student-cell"><span class="avatar">{{ strtoupper(substr($student->first_name,0,1).substr($student->last_name,0,1)) }}</span><div><b>{{ $student->full_name }}</b><small>{{ $student->email ?: 'No email' }}</small></div></div></td><td><code>{{ $student->student_id }}</code></td><td>{{ $student->course }}</td><td>Year {{ $student->year_level }}</td><td><span class="pill {{ $student->status }}">● {{ ucfirst($student->status) }}</span></td><td class="actions"><button wire:click="edit({{ $student->id }})">Edit</button><button class="danger" wire:click="delete({{ $student->id }})" wire:confirm="Delete this student and their attendance history?">Delete</button></td></tr>@empty<tr><td colspan="6" class="empty">No students found.</td></tr>@endforelse</tbody></table></div><div class="pagination">{{ $students->links() }}</div></div></div>
