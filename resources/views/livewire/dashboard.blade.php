<div><div class="hero"><div><span class="eyebrow light">Live campus overview</span><h2>Good {{ now()->hour < 12 ? 'morning' : 'afternoon' }}, {{ explode(' ',auth()->user()->name)[0] }}.</h2><p>Here’s how attendance is looking today.</p></div><a href="{{ route('kiosk') }}" class="hero-button" target="_blank">Launch kiosk <span>↗</span></a></div><div class="stats"><div class="stat"><span class="stat-icon mint">♙</span><div><small>Active students</small><strong>{{ number_format($totalStudents) }}</strong><em>Enrolled records</em></div></div><div class="stat"><span class="stat-icon green">✓</span><div><small>Present today</small><strong>{{ $presentToday }}</strong><em>{{ $totalStudents ? round($presentToday/$totalStudents*100) : 0 }}% attendance rate</em></div></div><div class="stat"><span class="stat-icon lime">↗</span><div><small>Checked out</small><strong>{{ $checkedOut }}</strong><em>{{ $presentToday-$checkedOut }} currently on campus</em></div></div></div><div class="grid-2"><div class="card"><div class="card-head"><div><span class="eyebrow">Weekly activity</span><h3>Attendance trend</h3></div><span class="badge">Last 7 days</span></div><div class="chart">@foreach($chart as $point)<div class="bar-wrap"><span>{{ $point['value'] }}</span><div class="bar" style="height:{{ max(5,$point['value']/$max*100) }}%"></div><small>{{ $point['label'] }}</small></div>@endforeach</div></div><div class="card"><div class="card-head"><div><span class="eyebrow">Live feed</span><h3>Recent activity</h3></div><a href="{{ route('attendance.logs') }}">View all →</a></div><div class="activity">@forelse($recent as $log)<div><span class="avatar">{{ strtoupper(substr($log->student->first_name,0,1).substr($log->student->last_name,0,1)) }}</span><p><b>{{ $log->student->full_name }}</b><small>{{ $log->time_out ? 'Checked out' : 'Checked in' }} · {{ ($log->time_out ?? $log->time_in)->diffForHumans() }}</small></p><span class="status-dot"></span></div>@empty<p class="empty">No attendance yet today.</p>@endforelse</div></div></div></div>
